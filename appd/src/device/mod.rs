// Device-specific protocol implementations live here.
// Each device type gets its own submodule.
// TODO: add e.g. `pub mod widget_v1;` when implementing real device protocols.

pub fn placeholder() {}
